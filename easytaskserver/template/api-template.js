var debug = require('debug')('ets:api');
var taskdb = requrire('../modules/taskdb.js');

module.exports = function (req, res, next) {
        input  -> req.body
        req.body.name
        check!

        debug(sldgj);
        XXXXXXXXXXXXXXXXXXXXXXXXXXX

        res.ret = json{}
        res.success = false/ true;
        res.message = ""
        next();
};
